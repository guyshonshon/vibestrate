node_modules/
dist/
coverage/
.amaco/runs/
.amaco-worktrees/
*.log
.DS_Store
.env
.env.*
!.env.example
.vitest-cache/
.tsbuildinfo

# Local agent operating protocol — kept out of the (public) repo.
CLAUDE.md

# Scheduler runtime state — changes on every run, not source.
.amaco/scheduler/lock
.amaco/scheduler/state.json
.amaco/scheduler/*.ndjson

# Transient dashboard run-spec handoff files (deleted by run-entry on read).
.amaco/.run-spec-*.json

# Notification runtime state — changes as runs emit/receipt notifications.
.amaco/notifications/notifications.json
.amaco/notifications/receipts.json

# Stray local asset.
logo-text.png

# Local scratch / brainstorm notes (not part of the repo)
names.txt
TODO_NEW*.md
TODO-V3.md
