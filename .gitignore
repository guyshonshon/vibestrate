node_modules/
dist/
coverage/
.amaco/runs/
.amaco-worktrees/
*.log
.DS_Store
.env
.env.*
!.env.example
.vitest-cache/
.tsbuildinfo

# Local agent operating protocol — kept out of the (public) repo.
CLAUDE.md

# Scheduler runtime state — changes on every run, not source.
.amaco/scheduler/lock
.amaco/scheduler/state.json
.amaco/scheduler/*.ndjson

# Stray local asset.
logo-text.png
